import { Link, useRouterState } from "@tanstack/react-router";
import { Bell, Boxes, CreditCard, History, LayoutDashboard, Menu, Package, Search, Settings, ShoppingCart, Users, X } from "lucide-react";
import { useState, type ReactNode } from "react";
import { Brand } from "./brand";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
const nav=[{to:"/app",label:"Dashboard",icon:LayoutDashboard},{to:"/app/produits",label:"Produits",icon:Package},{to:"/app/stock",label:"Stock",icon:Boxes},{to:"/app/ventes",label:"Ventes",icon:ShoppingCart},{to:"/app/clients",label:"Clients",icon:Users},{to:"/app/credits",label:"Crédits",icon:CreditCard},{to:"/app/historique",label:"Historique",icon:History},{to:"/app/parametres",label:"Paramètres",icon:Settings}] as const;
export function AppShell({children}:{children:ReactNode}){const path=useRouterState({select:s=>s.location.pathname});const[open,setOpen]=useState(false);return <div className="min-h-screen bg-app"><aside className="fixed inset-y-0 left-0 z-40 hidden w-60 border-r border-sidebar-border bg-sidebar px-4 py-5 lg:block"><Brand/><div className="mt-8 rounded-md bg-sidebar-accent p-3"><p className="text-[11px] text-muted-foreground">Boutique actuelle</p><b className="mt-1 block text-xs">Tech Sénégal — Dakar</b></div><nav className="mt-5 space-y-1">{nav.map(({to,label,icon:Icon})=><Link key={to} to={to} activeOptions={{exact:to==="/app"}} className={path===to?"app-nav app-nav-active":"app-nav"}><Icon/>{label}</Link>)}</nav><div className="absolute inset-x-4 bottom-5 rounded-md border border-sidebar-border p-3"><p className="text-xs font-semibold">Amadou Ndiaye</p><p className="text-[11px] text-muted-foreground">Propriétaire</p></div></aside>{open&&<div className="fixed inset-0 z-50 lg:hidden"><button className="absolute inset-0 bg-overlay" aria-label="Fermer" onClick={()=>setOpen(false)}/><aside className="relative h-full w-72 bg-sidebar p-5 shadow-xl"><div className="flex items-center justify-between"><Brand/><Button variant="ghost" size="icon" onClick={()=>setOpen(false)}><X/></Button></div><nav className="mt-8 space-y-1">{nav.map(({to,label,icon:Icon})=><Link key={to} to={to} onClick={()=>setOpen(false)} className={path===to?"app-nav app-nav-active":"app-nav"}><Icon/>{label}</Link>)}</nav></aside></div>}<div className="lg:pl-60"><header className="sticky top-0 z-30 grid h-16 grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-3 border-b border-border bg-card/95 px-4 backdrop-blur md:px-6"><Button variant="ghost" size="icon" className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></Button><div className="relative hidden max-w-md md:block"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground"/><Input className="pl-9" placeholder="Rechercher un produit, une vente..."/></div><div className="md:hidden"><Brand compact/></div><div className="flex items-center gap-2"><Button variant="ghost" size="icon" aria-label="Notifications"><Bell/></Button><button className="hidden rounded-md border border-border px-3 py-2 text-xs font-semibold sm:block">Tech Sénégal — Dakar ▾</button><span className="grid size-9 place-items-center rounded-full bg-primary text-xs font-bold text-primary-foreground">AN</span></div></header><main className="p-4 pb-24 md:p-6 lg:p-8 lg:pb-8">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-30 grid grid-cols-5 border-t border-border bg-card px-2 py-2 lg:hidden">{nav.slice(0,5).map(({to,label,icon:Icon})=><Link to={to} key={to} className={path===to?"mobile-nav text-primary":"mobile-nav text-muted-foreground"}><Icon/><span>{label}</span></Link>)}</nav></div>}
