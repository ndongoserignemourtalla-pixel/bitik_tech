import { Link } from "@tanstack/react-router";import type{ReactNode}from"react";import { Brand } from "./brand";
const steps=["Entreprise","Logo","Boutique","Configuration","Terminé"];
export function OnboardingShell({step,children}:{step:number;children:ReactNode}){return <main className="min-h-screen bg-app"><header className="border-b border-border bg-card"><div className="container-shell flex h-16 items-center justify-between"><Brand/><Link to="/connexion" className="text-sm font-medium text-muted-foreground">Quitter</Link></div></header><div className="mx-auto max-w-5xl px-4 py-8 lg:py-12"><div className="mb-10 grid grid-cols-5 gap-2">{steps.map((s,i)=><div key={s} className="min-w-0"><div className={i<=step?"h-1 rounded bg-primary":"h-1 rounded bg-border"}/><p className={i===step?"mt-2 truncate text-xs font-bold text-foreground":"mt-2 truncate text-xs text-muted-foreground"}>0{i+1} <span className="hidden sm:inline">{s}</span></p></div>)}</div><div className="mx-auto max-w-2xl">{children}</div></div></main>}
export function Field({label,...props}:React.InputHTMLAttributes<HTMLInputElement>&{label:string}){return <label className="grid gap-2 text-sm font-semibold">{label}<input className="h-11 rounded-md border border-input bg-background px-3 font-normal outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/15" {...props}/></label>}
